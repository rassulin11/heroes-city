{
  "name": "legends-city",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "npm-check-updates": "^3.0.8",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "concurrently --kill-others \"npm run start\" \"npm run apiserver\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "apiserver": "json-server --watch --port 3001 db.json"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "axios": "^0.18.0",
    "concurrently": "^4.1.0"
  }
}
